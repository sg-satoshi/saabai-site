"use client";

import PeterAvatarWidget from "../components/PeterAvatarWidget";

// This page is loaded inside a small bottom-right iframe on plasticonline.com.au.
// The iframe is sized to fit the widget — no full-page overlay needed.
export default function RexWidgetPage() {
  return <PeterAvatarWidget />;
}
